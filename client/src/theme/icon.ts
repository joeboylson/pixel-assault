import { IconProps } from "@phosphor-icons/react";
import { getTheme } from ".";

export const iconProps: IconProps = {
  size: 32,
  weight: "duotone",
  color: getTheme().BACKGROUND_COLOR_LIGHTER,
};
